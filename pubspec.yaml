name: search_ahead
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  json_annotation: ^4.4.0

  extended_image:
    git:
      url: https://github.com/mono0926/extended_image
      ref: dev

  retry: ^3.1.0
  dio: ^4.0.4
  dio_cache_interceptor: ^3.2.6

  dartz: ^0.10.1

  auto_route: ^3.2.4

  flutter_bloc: ^8.0.1
  bloc: ^8.0.3

  equatable: ^2.0.3

  logger: ^1.1.0

  shimmer: ^2.0.0

  intl: ^0.17.0

  hive_flutter: ^1.1.0

  shared_preferences: ^2.0.15

  # ServiceLocator
  get_it: ^7.2.0

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4
  json_serializable: ^6.1.5
  build_runner: 2.1.7
  hive_generator:
  auto_route_generator: ^3.2.3

  # Testing
  bloc_test: ^9.0.3
  mockito: ^5.1.0

flutter:
  uses-material-design: true
